## Summary

<!-- What does this PR change and why? Reference the issue it closes, if any: Closes #N -->

## Scope

<!-- Which workspace members are affected (cordis-core, cordis-timer, ...)? -->

## Checklist

- [ ] Changes include tests for the new behavior or fix
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo check --workspace --all-targets --locked` passes
- [ ] `cargo test --workspace --all-targets --locked` passes
- [ ] `cargo clippy --workspace --all-targets --locked -- -D warnings` passes
- [ ] Documentation (README / doc comments) updated if public behavior changed
