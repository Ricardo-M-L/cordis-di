[package]
name = "cordis-di-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Core of the cordis-di framework — Fiber, Context, Events, Registry, Reflect, Logger"
license = "MIT"
homepage = "https://github.com/Ricardo-M-L/cordis-di"
repository = "https://github.com/Ricardo-M-L/cordis-di"
readme = "../README.md"
keywords = ["cordis-di", "framework", "fiber", "events", "logger"]
categories = ["asynchronous", "web-programming", "development-tools"]

[dependencies]
serde = "1"
serde_json = "1"
tokio = { version = "1", features = ["rt", "time", "sync"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "rt-multi-thread", "time", "macros"] }
